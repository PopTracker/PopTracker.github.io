source "https://rubygems.org"

# NOTE: github-pages plugin is required for local jekyll to behave the same as
#       a github pages build.
#       Run/test locally with `bundle exec jekyll serve --watch --safe`

group :jekyll_plugins do
  gem 'github-pages'
end
